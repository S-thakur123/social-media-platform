spring:
  application:
    name: chat-service
  data:
    mongodb:
      # We use variables for the username, password, host, and database name
      uri: mongodb://${MONGO_USER}:${MONGO_PASS}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${CHAT_DB_NAME:chat_db}?authSource=admin
      